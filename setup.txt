Create a new AAD tenant
Create a new user on that tenant and make them global admin
Turn off security default
Get an E3 license trial
Got to Power Automate -> Monitor > Machine, and set up a new Dataverse
Go to Desktop flow runs -> start trial

Win 11 ent
Power Automate Desktop was already there. Did need an update.
Sign in w/ attacker creds
Machine-runtime app is not installed. So you can only build things by default, not run them remotely. Actually, cannot even run them.
Need to create an env per victim otherwise they see each other.
Reuse session (on cloud)